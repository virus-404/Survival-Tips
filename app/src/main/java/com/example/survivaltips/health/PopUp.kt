package com.example.survivaltips.health

import android.app.Dialog
import android.os.Bundle

class PopUp (a: Activity) : Dialog(a) {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
